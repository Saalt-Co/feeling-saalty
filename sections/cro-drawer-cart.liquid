<!-- CRO Cart DRAWER -->
{%- if template contains 'cart' -%}
    <!-- CRO Cart PAGE -->
{%- else -%}
    <!-- CRO NOT Cart PAGE -->
{%- endif -%}

{% schema %}
  {
    "name": "Cart Drawer",
    "settings": [
    ],
    "blocks":[
    ],
    "presets": [
      {
        "category": "Footer",
        "name": "CRO Cart Drawer"
      }
    ],
    "enabled_on": {
      "groups": ["footer"]
    }
  }
{% endschema %}